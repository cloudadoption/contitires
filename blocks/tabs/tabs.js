/**
 * Tabbed panels: one panel at a time, the way live's /media shows its logos
 * and its tire images.
 * @param {Element} block the tabs block
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
}
